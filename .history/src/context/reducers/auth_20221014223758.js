export const initialState = {
  loading: false,
  error: null,
  token: null,
  userId,
};

const authReducer = (state, { payload, type }) => {
  switch (type) {
    case 'value':
      break;

    default:
      break;
  }
};

export default authReducer;
