const initialOrderState = {
  loading: false,
  error: null,
  orders: null,
};

const orderReducer = (state, { type, payload }) => {
  switch (type) {
    case FET:
      break;

    default:
      break;
  }
};
