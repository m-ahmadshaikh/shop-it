const initialOrderState = {
  loading: false,
  error: null,
  success: false,
};

const orderReducer = (state, { type, payload }) => {
  switch (type) {
    case ORDER:
      break;

    default:
      break;
  }
};
